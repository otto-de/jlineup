{
  "name": "jlineup-core",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "<rootDir>/src/test/js/**/*.test.js"
    ]
  }
}
