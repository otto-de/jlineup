jlineup:
  installedBrowsers: chrome, chrome-headless, PhantomJS

server:
  servlet:
    context-path: /jlineup-test-contextpath

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    loggers:
      enabled: true