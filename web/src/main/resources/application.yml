jlineup:
  workingDirectory: /tmp/jlineup/
  screenshotsDirectory: report-{id}
  reportDirectory: report-{id}
  maxParallelJobs: 1

edison:
  status:
    application:
      description: Takes screenshots of configured webpages and checks for differences
